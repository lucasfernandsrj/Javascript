const soma = 10 + 10;
const multiplicacao = 10 * 10;
const operacao = (soma + multiplicacao) + 10; 

/* Importante: No caso de números, é possível salvar 
em uma variável não somente o número em si, mas 
também o resultado de uma operação (ou mais)*/

console.log(operacao);